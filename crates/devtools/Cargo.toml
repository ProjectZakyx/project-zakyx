[package]
name = "projekt-ora-devtools"
version = "0.1.0"
edition = "2021"

[dependencies]
projekt-ora-core = { path = "../core" }
windows = { version = "0.61.1", features = [
    "Win32_Foundation",
    "Win32_System_Com",
    "Win32_UI_WindowsAndMessaging"
] }
